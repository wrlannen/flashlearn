# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify OpenAI model (defaults to gpt-4o if not set)
OPENAI_MODEL=gpt-4o

# Optional: Server port (defaults to 3000 if not set)
PORT=3000
